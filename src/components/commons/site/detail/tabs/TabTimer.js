export default class TabTimer {
  static myInstance = null;

  static timer = null;

  static timerControl = null;

  /*

  static getInstance() {
      if (TabTimer.myInstance == null) {
        TabTimer.myInstance = new TabTimer();
      }
      return TabTimer.myInstance;
  }

  getTimer() {
      return this.timer;
  }

  setTimer(id) {
      this.timer = id;
  }
  
  getTimerControl() {
      return this.timer;
  }

  setTimerControl(id) {
      this.timer = id;
  }

  */
}
